## What does this change?

<!-- One or two sentences. / Одно-два предложения. -->

## What measurement backs it?

<!-- This project's rule: every fix carries a measurement. A probe output, a suite that
     now fails without the change, a diff against a clean browser. -->

## Checklist

- [ ] `npm test` passes (the 8 Node suites)
- [ ] `npm run test:all` passes, or I said which browser suites I could not run
- [ ] If I edited `mw/`, I ran `npm run build` so the bundle is not stale
- [ ] If this changes what a site can read, the README's Limits section still reads true
